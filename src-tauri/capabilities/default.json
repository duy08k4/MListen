{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "opener:default",
    "fs:default",
    "fs:scope-document-recursive",
    "fs:allow-create",
    "fs:allow-write-text-file",
    "fs:allow-read-text-file",
    "fs:allow-remove",
    {
      "identifier": "fs:scope",
      "allow": [
        {
          "path": "$DOCUMENT"
        },
        {
          "path": "$DOCUMENT/**"
        },
        {
          "path": "$DOCUMENT/mlisten/**"
        },
        {
          "path": "$DOCUMENT/mlisten/set/**"
        }
      ]
    }
  ]
}